info:
  name: Delete First task
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8080/api/tasks/First task
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
