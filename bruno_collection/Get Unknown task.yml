info:
  name: Get Unknown task
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8080/api/tasks/Unknown task
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
