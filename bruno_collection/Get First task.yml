info:
  name: Get First task
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8080/api/tasks/First task
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
