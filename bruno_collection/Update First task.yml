info:
  name: Update First task
  type: http
  seq: 9

http:
  method: PUT
  url: http://localhost:8080/api/tasks/First task
  body:
    type: json
    data: |-
      {
        "description" : "This is the absolutely first task"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
