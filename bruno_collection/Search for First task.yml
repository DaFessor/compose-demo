info:
  name: Search for First task
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:8080/api/tasks/search?q=task
  params:
    - name: q
      value: task
      type: query
  body:
    type: json
    data: |-
      {
        "q" : "First"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
